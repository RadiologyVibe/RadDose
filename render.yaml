services:
  - type: web
    name: raddose
    runtime: python
    plan: free
    buildCommand: pip install -e .
    startCommand: uvicorn raddose.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
